import { logger } from "@util/Logging";
import { BaseResource } from "./BaseResource";

export class TierOneResource extends BaseResource {
    constructor(scene) {
        super(scene, 'TierOne')
    }
}

